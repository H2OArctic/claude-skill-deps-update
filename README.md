# claude-skill-deps-update

Скилл для Claude Code: обновление зависимостей в Bun-проектах и монорепо без сюрпризов.

Обычное «обнови библиотеки» ломается на деталях, которые не видны в `bun outdated`:

- одна библиотека сидит в десятке `package.json` с разными диапазонами — в дереве оказываются две её копии;
- каретка на `0.x` разрешает только тот же минор, поэтому смена минора — фактический мажор, а выглядит как минор;
- часть версий запиннена точно намеренно — расширять диапазон нельзя, там уже ловили несовместимость;
- пакет вообще нельзя поднять, потому что его держит `peerDependencies` родителя;
- корневые `overrides` живут своей жизнью: часть уже не нужна, часть придётся добавить после `bun audit`,
  часть вообще не доезжает до дерева (Bun переопределяет не все ветки), а часть, наоборот, тихо **держит
  версию ниже** той, что просит сам проект;
- свежий релиз популярного пакета — это ещё и вектор атаки: скомпрометированные версии снимают в первые
  сутки, а «поставить latest сразу» этого не различает;
- после правок нужен честно пересобранный lockfile, иначе проверки идут против старого дерева;
- «этот пакет пока не трогаем» — решение, а не настроение: без журнала тот же вопрос задаётся снова
  на каждом прогоне, а когда причина отказа исчезает, об этом никто не вспоминает.

Скилл разбирает всё это по фазам и заканчивается прогоном typecheck и тестов.

Начиная с 2.0 требуется **Bun ≥ 1.4**: дерево читается прямо из `bun.lock`, минимальные безопасные версии
считает `bun audit fix --dry-run`, дубли — `bun dedupe --dry-run`, а что изменилось между версиями пакета
показывает `bun pm diff` (включая добавленный `postinstall` и новые импорты `fs`/`child_process`).

## Установка

```bash
git clone git@github.com:H2OArctic/claude-skill-deps-update.git ~/Developer/claude-skill-deps-update
cd ~/Developer/claude-skill-deps-update
bun install
bun bin/cli.ts install
bun bin/cli.ts hook        # автообновление, см. ниже
```

`install` кладёт симлинк `~/.claude/skills/deps-update` → `<репозиторий>/skill`, поэтому свежий коммит
становится скиллом сразу, без переустановки. Нужна независимая копия — `bun bin/cli.ts install --copy`.

В Claude Code скилл вызывается как `/deps-update`.

## Обновление

`hook` прописывает в `~/.claude/settings.json` хук `SessionStart`, который при старте сессии тихо
делает `git fetch` + fast-forward — руками `git pull` дёргать не нужно:

- проверка не чаще раза в 6 часов (метка в `~/.cache/claude-skill-deps-update/`, отдельная на каждый путь установки),
  так что обычный старт ничего не делает и ничего не стоит;
- `async: true` — сессия не ждёт сеть;
- молчит всегда, кроме факта обновления: одна строка `Скилл deps-update обновлён до X (a → b)`;
- незакоммиченные изменения или невозможный fast-forward — обновление пропускается, разбирается человек;
- выключить: `bun bin/cli.ts hook --remove`.

Настройки читаются и пишутся целиком, прежняя версия остаётся рядом как `settings.json.bak`,
права файла сохраняются.

Вручную — когда угодно:

```bash
cd ~/Developer/claude-skill-deps-update
bun bin/cli.ts update           # git pull --ff-only + актуализация установки
bun bin/cli.ts selfupdate --force   # то же, что делает хук, но игнорируя интервал
```

## Команды

| Команда | Что делает |
|---|---|
| `bun bin/cli.ts install [--copy] [--force] [--skills-dir <dir>] [--target <dir>]` | Установка (симлинк или копия) |
| `bun bin/cli.ts update` | `git pull --ff-only`, обновление копии, проверка симлинка |
| `bun bin/cli.ts selfupdate [--max-age <sec>] [--force] [--quiet]` | Тихий fetch + fast-forward, для хука |
| `bun bin/cli.ts hook [--remove]` | Включить/выключить автообновление через `SessionStart` |
| `bun bin/cli.ts status` | Где установлен, какая ревизия, состояние автообновления, версия Bun |
| `bun bin/cli.ts uninstall` | Снять установку (файлы репозитория не трогает) |
| `bun bin/cli.ts scan [args...]` | Запустить анализатор в текущем проекте |

Журнал отложенных решений — `skill/scripts/decisions.ts`, см. раздел ниже.

## Анализатор

`skill/scripts/deps-scan.ts` — read-only инвентаризация воркспейса: ничего не ставит, не правит
`package.json` и не трогает lockfile. Запускать из корня проверяемого проекта:

```bash
bun ~/Developer/claude-skill-deps-update/skill/scripts/deps-scan.ts
```

Флаги: `--json` (машинный вывод), `--no-net` (без запросов в registry и без `bun audit`), `--no-tree`
(не читать `bun.lock` — быстрее, но без вердиктов и фактических версий; старое имя `--no-why` работает),
`--min-age <дней>` (помечать слишком свежие релизы; по умолчанию берётся из `bunfig.toml`),
`--cwd <path>` (другой корень), `--decisions <path>` (журнал отложенных решений).

Дерево читается напрямую из `bun.lock` через `Bun.JSONC.parse` — того же источника, что и у `bun pm ls`,
но со структурой: путь установки, кто требует пакет и какая копия ему фактически досталась. Ни разбора
ASCII-дерева, ни десятков вызовов `bun why`: монорепозиторий на несколько тысяч пакетов разбирается
целиком за считанные секунды, из которых почти всё — походы в registry.

Что выдаёт:

1. **Расхождения версий** — одна библиотека с разными диапазонами по воркспейсам (`peerDependencies`
   внутренних пакетов не считаются расхождением: они намеренно широкие). Для повторяющихся случаев
   подсказывается каталог: одна запись версии в корне, `"catalog:"` в воркспейсах.
2. **Дубли в дереве** — объявленный пакет установлен в нескольких версиях сразу, плюс вывод
   `bun dedupe --dry-run`: что схлопнется без единой правки `package.json`. Отдельно помечаются
   понижения версии — dedupe переиспользует то, что уже есть в lockfile, и после него нужен новый `bun audit`.
   Транзитивные дубли — счётчиком: их в монорепо десятки, и это обычно норма.
3. **Кандидаты на обновление** с классом риска: `PATCH`, `MINOR`, `MAJOR`, `0.x MINOR — ЛОМАЮЩЕЕ`,
   `0.0.x PATCH — ЛОМАЮЩЕЕ`, `PRERELEASE`. Рядом — что записано в `package.json`, что реально стоит
   в дереве, максимум внутри диапазона (подтянется сам при пересборке lockfile), возраст latest
   и пометка 🛡 при открытых advisory. Если latest уже установлен, это помечается: правка диапазона
   там ничего не обновляет.
   Отложенные пакеты сюда не попадают — они уходят в разделы **3a «Пора вернуться»** и
   **3b «Отложено — не спрашивать»** (см. журнал ниже).
4. **Семейства** — скоуп (`@scope/*`) или фреймворк вместе со своими плагинами и адаптерами.
   Обновлять только целиком: полуобновлённое семейство падает не на установке, а в рантайме.
5. **Overrides / resolutions** — все четыре формы записи (верхнеуровневая, `parent>child`,
   `parent@^4>child`, `parent/child`, вложенная форма npm), число копий, доля потребителей, до которых
   правило реально доехало, и вердикт по каждому:
   - `REDUNDANT_NOW` — потребители сами требуют не меньше, кандидат на удаление;
   - `RAISES_FLOOR` — поднимает минимум внутри допустимого диапазона, похоже на security-override;
   - `BREAKS_CEILING` — кому-то досталась версия выше объявленного им потолка; если «достаёт» не 100%,
     часть веток осталась на своей копии и потолок им никто не ломал;
   - `CAPS_BELOW` — override держит версию **ниже** той, что просит потребитель: так тихо режется
     обновление, когда пакет есть и в `dependencies`, и в `overrides`;
   - `NOT_APPLIED` — ни один потребитель не получил версию из override: lockfile разошёлся с `package.json`;
   - `NOT_IN_TREE` — пакета в дереве нет вовсе, правило мёртвое;
   - плюс пометка, если пакет есть и в `overrides`, и в прямых зависимостях — такие правятся синхронно,
     и предупреждение, если блок в `package.json` разошёлся с `bun.lock`.
6. **Уязвимости и план `bun audit fix --dry-run`** — advisory по пакетам и минимально достаточные
   безопасные версии, посчитанные Bun с учётом диапазонов всех зависимых (не latest), плюс то, что
   он закрыть не может: заблокировано чужим диапазоном или безопасной версии не существует.
   6a. **Слишком свежие релизы** — при заданном пороге: что опубликовано меньше N дней назад.
7. **Deprecated** пакеты, у которых сам `latest` помечен устаревшим.
8. **Ручная проверка** — неразобранные диапазоны и ошибки registry: скрипт не угадывает.

Вердикт по override считается по каждому потребителю отдельно: его объявленный диапазон против той копии,
которая ему фактически досталась по `bun.lock`. Это уже не гипотеза по диапазонам, но всё ещё описание
текущего дерева. Что override держит на самом деле, показывает только эксперимент: снять все overrides
разом → `bun install` → сравнить `bun audit` и число копий.

Причём эксперимент имеет смысл **только в самом конце** — после обновления версий и полной пересборки
lockfile. До этого он описывает дерево, которого к концу работы не будет: обновлённый родитель может сам
начать требовать исправленную версию, и половина overrides отваливается именно поэтому. Поэтому в скилле
overrides — отдельная фаза после lockfile, а не часть планирования.

## Журнал отложенных решений

Отказ «пока не обновляем» пишется в `.claude/deps-update.json` **проверяемого проекта** — вместе с
условием, при котором к вопросу нужно вернуться. Файл предназначен для коммита: это ответ команды,
а не кэш.

```bash
S=~/.claude/skills/deps-update/scripts/decisions.ts

bun $S list                                    # что отложено и почему
bun $S defer react --declined 19.0.0 \
     --reason "ждём, пока экосистема догонит" \
     --blocked-by react-dom                    # вернуться, когда react-dom отпустит
bun $S defer chalk --declined 5.0.0 \
     --reason "ESM-only" --after 2026-10-01     # вернуться по дате
bun $S resume react                            # решили заняться — вопрос вернётся
bun $S prune                                   # убрать записи о пакетах, которых уже нет
```

Условия возврата проверяет анализатор и раскладывает отложенное на два раздела:

- **«Пора вернуться»** — причина отказа изменилась: названный блокер больше не ограничивает пакет
  (по внешним потребителям из `bun.lock`, требования своих же воркспейсов не считаются), вышел мажор выше
  отклонённого, истёк срок или проект уже сам ушёл дальше отклонённой версии — тогда запись помечается
  как устаревшая.
- **«Отложено — не спрашивать»** — причина держится; в строке видно, кто именно держит
  (`react-dom@18.3.1 requires ^18.3.1`). Такие пакеты не попадают в кандидатов, и скилл не задаёт
  вопрос повторно.

`--declined` принимает точную версию (`19.0.0`), а не диапазон: по её мажору анализатор решает, считать ли
следующий мажор новым вопросом.

Отсутствие условий (`defer` без `--blocked-by`/`--after`) означает «не спрашивать, пока не скажу» —
вернуть вопрос можно только через `resume`.

## Требования

- **Bun ≥ 1.4** — нужны `Bun.JSONC` (чтение `bun.lock`), `Bun.semver`, `bun audit fix`, `bun dedupe`,
  `bun pm diff` и `--minimum-release-age`. Разрабатывалось и проверялось на 1.4.0.
  На 1.2/1.3 анализатор откажется запускаться: без этих команд половина выводов была бы догадкой.
- Проверяемый проект должен использовать текстовый `bun.lock`. Остался бинарный `bun.lockb` —
  `bun install --save-text-lockfile`.
- Проект с `package.json`; `workspaces` не обязательны — для одиночного пакета работает так же.
- Доступ к npm registry (или свой через `NPM_CONFIG_REGISTRY`).

## Разработка

```bash
bun test           # тесты semver-ядра, чтения bun.lock и разбора overrides
bun run typecheck  # tsc --noEmit по всему репозиторию
```

## Структура

```
bin/cli.ts                       установка/обновление/статус/снятие/scan
skill/SKILL.md                   инструкции скилла: фазы, группы риска, запреты
skill/scripts/deps-scan.ts       анализатор (read-only)
skill/scripts/decisions.ts       журнал отложенных решений (list/defer/resume/prune)
skill/scripts/lib/semver.ts      версии и диапазоны поверх Bun.semver
skill/scripts/lib/lockfile.ts    разбор bun.lock: копии, потребители, разрешение путей
skill/scripts/lib/overrides.ts   нормализация всех форм записи overrides
tests/                           bun test для этих трёх модулей
```

## Лицензия

MIT
